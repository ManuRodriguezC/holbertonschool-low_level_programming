#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *main - print name
 *@argc: number caracters
 *@argv: caracter
 *Return: 0
 */
int main(int argc, char *argv[])
{
int i, j;

	for (i = 0 ; i < argc ; i++)

	for (j = 0 ; j < *argv[j] ; j++)

printf("%d", argc);

return (0);
}
