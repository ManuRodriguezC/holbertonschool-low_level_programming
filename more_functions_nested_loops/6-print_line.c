#include "main.h"

/**
 *print_line - line line line
 *@n: number is times is repeted
 *
 */
void print_line(int n)
{
while (n != 0)
	{
	if (n > 0)
	{
		_putchar('_');
		n--;
if (n <= 0)
	
	}
	}
_putchar('\n');
}
