#include "main.h"
#include <stdio.h>

/**
 * *_memcpy - copy n bytes
 *@des: destin
 *@src: orogin
 *@n: bytes
 *Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
int i;

while (s[i] < n && s[i] != '\0')
{
	dest = s[i];
	i++;
}
return (dest);
}
