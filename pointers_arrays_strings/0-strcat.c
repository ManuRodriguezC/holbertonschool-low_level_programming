#include "main.h"
#include <stdio.h>

/**
 * *_strcat - concatenar
 *@dest: destin
 *@src: inicio
 *Return: dest
 */
char *_strcat(char *dest, char *src)
{
int i, j;

for (i = 0 ; dest[i] != '\0' ; i++)
{
}	
for (j = 0 ; (dest[i + j] = *src++) != '\0' ; j++)
{
}
return (dest);
}
