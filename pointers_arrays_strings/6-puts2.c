#include "main.h"
#include <stdio.h>

/**
 *puts2 - print string x two
 *@str: string
 *
 */
void puts2(char *str)i
{
int i, l;
int t = 0;

for (i = 0 ; str[i] != '\0' ; i++)
	if (i == 0 && i == % 2)
	_putchar(str[i]);
}
