#include "lists.h"

/**
 *listint_len - Number of elements of the list
 *@h: Singly linked list
 *Return: count is number of elements
 */
size_t listint_len(const listint_t *h)
{
size_t count = 0;

for (; h; count++)

return (count);
}
