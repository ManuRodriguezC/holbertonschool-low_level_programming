#include "main.h"
#include <stdio.h>

/**
 *factorial - factorial of a number
 *@n: number
 *Return: -1, n
 */
int factorial(int n)
{
int resp;
if (n < 0)
	return (-1);
if
resp = factorial(n -1)*n
	return (resp);
}
