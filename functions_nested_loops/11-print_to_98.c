#include "main.h"
#include <stdio.h>
/**
 *print_to_98 - conteo
 *@n: number diferent
 *Return: 0
 */
void print_to_98(int n)

if (n < 98)
	{
		n++;
	}
else
	if (n > 9)
	{
		number--;
	}
printf("%i", n);
printf(",");
printf(" ");
return (0);
}
