#include <stdio.h>
#include "3-calc.h"

/**
 *(*get_op_func - call fucntions)
 *@s: character
 *Return:s
 */
int (*get_op_func(char *s))(int, int)
{
char funciones = "+,-,/,*,%";
char +, -, *, /, %;
+= *op_add;
-= *op_sub;
/= *op_div;
*= *op_mul;
%= *op_mod;

if (s == funciones)
	return (s);
}
